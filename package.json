{
  "name": "todo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "run-p dev:*",
    "dev:server": "bun --watch run src/server/index.tsx",
    "dev:client": "bun build src/entry-client.tsx --outdir dist/public --watch",
    "dev:css": "bunx tailwindcss -i src/index.css -o dist/public/styles.css --watch",
    "build": "run-s build:client build:css",
    "build:client": "bun build src/entry-client.tsx --outdir dist/public --minify",
    "build:css": "bunx tailwindcss -i src/index.css -o dist/public/styles.css --minify",
    "start": "NODE_ENV=production bun run src/server/index.tsx",
    "check": "run-s check:lint check:format check:types",
    "check:fix": "run-s check:lint:fix check:format:write check:types",
    "check:lint": "biome check .",
    "check:lint:fix": "biome check --fix .",
    "check:format": "biome format .",
    "check:format:write": "biome format --write .",
    "check:types": "tsc --noEmit",
    "db:generate": "bunx --bun prisma generate",
    "db:migrate": "bunx --bun prisma migrate dev",
    "db:push": "bunx --bun prisma db push",
    "db:studio": "bunx --bun prisma studio"
  },
  "dependencies": {
    "@elysiajs/static": "^1.4.7",
    "@prisma/client": "^7.3.0",
    "elysia": "^1.4.22",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.13",
    "@tailwindcss/cli": "^4.1.18",
    "@types/bun": "latest",
    "@types/react": "^19.2.10",
    "@types/react-dom": "^19.2.3",
    "npm-run-all": "^4.1.5",
    "prisma": "^7.3.0",
    "tailwindcss": "^4.1.18"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
